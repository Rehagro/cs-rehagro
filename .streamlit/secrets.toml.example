# Renomeie este arquivo para `secrets.toml` e coloque os valores reais.
# O arquivo `secrets.toml` está no .gitignore.
#
# Em produção (Streamlit Cloud), configure em Settings → Secrets (mesmo formato).
CS_PASSWORD = "troque-essa-senha"
